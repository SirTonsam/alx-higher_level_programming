#!/usr/bin/python3

# Import the add function from the add_0.py file
from add_0 import add

# Assign the value 1 to a variable called a
a = 1

# Assign the value 2 to a variable called b
b = 2

# Call the add function with the values of a and b as arguments
result = add(a, b)

# Print the result of the addition
print(f"{a} + {b} = {result}")
